@model IEnumerable<MvcRPG.Models.Axe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Enchantment.Element)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attack)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Handler)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Enchantment.Element)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Attack)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Handler)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AxeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AxeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AxeId })
        </td>
    </tr>
}

</table>
